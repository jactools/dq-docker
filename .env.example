# Example environment file for local development. Copy to `.env` and
# fill in values (the repository's `.env` is gitignored).

# Azure Service Principal credentials (used by the runtime to authenticate)
AZURE_TENANT_ID="<Your-Azure-Tenant-ID>"
AZURE_CLIENT_ID="<Your-Azure-Client-ID>"
AZURE_CLIENT_SECRET="<Your-Azure-Client-Secret>"

# Storage account and container
ADLS_ACCOUNT_NAME="<Your-Storage-Account-Name>"
ADLS_CONTAINER_NAME="<Your-Container-Name>"

# Select the data source to use locally. This must match a YAML filename
# in `dq_docker/config/data_sources/` (filename without extension).
# Example: `dq_docker/config/data_sources/ds_sample_data.yml` -> `ds_sample_data`
DQ_DATA_SOURCE=ds_sample_data

# Optional: override project root inside containers or tests
# DQ_PROJECT_ROOT=/usr/src/app
