# Adapter Notes

This directory contains short, focused notes for each optional data-source adapter supported by the package. Each file documents
- install extra name
- minimal credential hints / environment variables
- common caveats or platform notes

Consult the individual files for adapter-specific guidance.
